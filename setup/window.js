class subWindow {
    constructor(windowPath, iconPath, options = { minWidth: 400, minHeight: 300 }) {
        // console.log(`Adding window: ${windowPath}, ${iconPath} with options`, options);
        this.windowPath = windowPath;
        this.iconPath = iconPath;
        this.options = options;
        
    }
}